# checksuite-timer

A simple github action for tracking PR checksuite duration.

Takes a wall-to-wall approach based on checks made on a given commit SHA, intended to represent the time an engineer spends waiting for checks to finish on that commit.
